# Hardware Power-On Self-Test Prototype

Throwaway terminal simulator for firmware POST behavior and degraded operating modes.

Run:

```bash
npm run prototype:logic-self-test
```

Toggle individual hardware faults, run POST, and inspect whether the device enters full service, degraded standalone mode, or service lockout.
