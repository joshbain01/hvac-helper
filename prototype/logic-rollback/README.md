# Partition Swap and Rollback Prototype

Throwaway terminal simulator for ESP32 OTA boot safety.

Run:

```bash
npm run prototype:logic-rollback
```

It explores pending partition flags, signature verification, self-test confirmation, watchdog rollback, and physical RA+SA rollback override.
