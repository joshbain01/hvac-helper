# FSM Webhook Mapper Prototype

Throwaway terminal simulator for mapping snapshots into ServiceTitan or Housecall Pro style payloads.

Run:

```bash
npm run prototype:logic-fsm-sync
```

It surfaces custom-field overflow, token expiry, queued retries, and idempotent snapshot submission.
